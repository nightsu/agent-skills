interface:
  display_name: "Git Commit Assistant"
  short_description: "安全审查改动、控制提交范围并生成 commit"
  default_prompt: "Use $git-commit to review my git diff, stage only the relevant files, split unrelated work if needed, and create a concise commit message."

policy:
  allow_implicit_invocation: true
