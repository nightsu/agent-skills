interface:
  display_name: "Markdown Lint"
  short_description: "清理并规范 Markdown 文件格式"
  default_prompt: "Use $markdown-lint to clean and normalize a Markdown file, fixing spacing, links, separators, table formatting, and common HTML export artifacts."
